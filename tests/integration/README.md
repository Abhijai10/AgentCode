# Integration Fixtures

This directory anchors cross-crate integration tests for AgentCode phase gates. Rust
workspace-level integration code lives inside crate `tests/` directories until a root
test harness crate is admitted.
