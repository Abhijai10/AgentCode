[package]
name = "ac-daemon"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
ac-config = { path = "../ac-config" }
ac-kernel = { path = "../ac-kernel" }
ac-logging = { path = "../ac-logging" }

[lints]
workspace = true
