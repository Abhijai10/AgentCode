# V1 Artifact Report

Release artifacts are identified by version, platform, kind, build hash, integrity
hash, source commit and creation time. V1 release uses only the artifact that passed
RC validation and final release approval.
